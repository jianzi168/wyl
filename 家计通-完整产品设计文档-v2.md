# 家计通·完整产品设计文档

> 版本：v2.0（修正版）  
> 状态：**已实现（v2.1.0 完整版，2026-06-06）**  
> 负责人：夏雨（产品设计）  
> 审核：叶纬（总负责人）、吴银龙（开发落地）  
> 修订依据：`家计通-产品设计评估报告.md`（2026-06-06）  
> 替代文档：`家计通-完整产品设计文档.md`（v1.0，已归档）

---

## 修订说明

本版基于业界对标评估收窄产品边界，明确 **MVP 与完整愿景分阶段交付**，核心变更如下：

| 变更项 | v1.0 | v2.0 |
|--------|------|------|
| 产品定位 | 新一代省钱记账工具（全能） | 记完就知道还能花多少的预算记账 App |
| Tab 结构 | 5 Tab（含攻略、工具） | MVP 3 Tab；v1.1 扩展洞察页 |
| 录入方式 | 纯手动 | 手动 + 账单导入 + 文本解析（MVP） |
| 性质三档 | 每笔强制选择 | 默认「可选」，事后可修正 |
| 省钱触达 | 18 种独立弹窗 | 统一洞察中心 + 内嵌反馈 |
| AI | 通用对话 + 多场景 | MVP 关键词分类；v1.2 周期性教练 |
| 优惠券 | 爬虫抓取 | 延后；仅官方联盟 API（v2.0+） |
| 成就 | 23 个徽章 | MVP 8 个；完整版扩展至 15 个 |
| 开发周期 | 58 天全集 | MVP 6–8 周；完整愿景 4–6 个月 |

---

## 一、项目概述

### 1.1 产品定位

**家计通** — 记完就知道还能花多少的记账 App

帮助用户在每一笔消费后立刻感知预算影响，用数据驱动省钱习惯，而不是记完就忘。

**对内一句话：** 不是「最全的记账 App」，而是 **「反馈最快的预算 App」**。

### 1.2 目标人群

**MVP 核心人群（优先）：**

- 想控制月支出的工薪阶层
- 有存钱目标（应急金、旅行、首付）的年轻人
- 曾使用过记账 App 但放弃的用户（手动记账疲劳）

**延展人群（v1.1+）：**

- 需要查看家庭汇总的有伴侣用户（只读共享，v1.2 协作记账）

### 1.3 核心价值主张

> 记了白记？是大多数记账软件的症结。  
> 家计通让每笔记账都有即时反馈——花完立刻知道还能花多少，超支有提醒，省钱看得见。

### 1.4 对标与差异化

| 维度 | 竞品常见做法 | 家计通差异化 |
|------|--------------|--------------|
| 记账速度 | 多字段、多步骤 | 金额 + 分类，性质默认可选 |
| 预算感知 | 月底报表才看 | 每笔完成后即时反馈 |
| 固定收支 | 每月手动画 | 设置一次，自动循环入账 |
| 分类数量 | 20–30 个 | 10 个主类，降低决策成本 |
| 省钱方式 | 导购弹券（广告感） | 基于账单的洞察建议（顾问感） |
| 数据录入 | 纯手动 | 支持微信/支付宝账单导入 |

**不对标：** 什么值得买（导购）、贝壳（房产）、通用 ChatGPT（闲聊）。

**应对标：** YNAB（预算方法论）、鲨鱼记账（录入速度）、Copilot Money（周期性洞察）。

---

## 二、版本规划

### 2.1 MVP（v1.0）— 6–8 周

**目标：** 验证「记账 → 即时反馈 → 留存」核心闭环。

| 模块 | 范围 |
|------|------|
| 记账 | 首页 Dashboard、QuickRecord、分类管理、固定收支 |
| 录入增强 | 微信/支付宝 CSV 导入、账单文本粘贴解析 |
| 预算 | 分类月预算、可选年度预算、模板引导 onboarding |
| 报表 | 收支三栏、环形图、近 6 月折线、支出下钻 |
| 洞察 | 记账完成页内嵌反馈；首页洞察卡片（非弹窗） |
| 账号 | 微信登录、手机号绑定、云同步、数据导出 |
| 成就 | 8 个核心徽章 |
| 合规 | 隐私政策、账号注销、离线缓存与冲突合并 |

**不包含：** 攻略 Tab、工具 Tab、AI 对话、LBS、比价、爬虫、家庭共享。

### 2.2 v1.1 — MVP 后 4 周

- 语音一句话记账
- 周报 / 月报（含日历热力图）
- 洞察中心 Inbox（统一触达，替代分散提醒）
- 支付后订阅消息提醒补记（微信小程序）

### 2.3 v1.2 — MVP 后 8 周

- AI 财务教练（周报/月报解读、异常支出问答，聚合调用）
- 成就扩展至 15 个
- 家庭只读共享（邀请查看汇总）

### 2.4 v2.0 — 完整愿景

- 省钱洞察页（习惯提醒 + 官方联盟 Deep Link，无爬虫）
- 轻量房贷/车贷计算器（无实时房价数据）
- 家庭协作记账
- 优惠券中心（仅官方联盟 API）

---

## 三、技术架构决策

| 事项 | 决策 | 备注 |
|------|------|------|
| 整体框架 | UniApp（Vue 3 + Pinia） | 小程序优先，后续扩展 App |
| 后端 | 云服务 BaaS | LeanCloud / 腾讯云开发 等 |
| 登录 | 微信登录 + 手机号绑定 | 微信 openId 为主键 |
| 数据同步 | 云端为主，本地 IndexedDB 缓存 | 离线可记账，联网合并 |
| 图表 | uCharts / F2 | 小程序性能优先，数据 >500 条抽样 |
| AI（v1.2+） | 可插拔统一接口层 | 仅传聚合数据，不传明细 |
| 账单导入 | 客户端解析 + 服务端去重 | MVP 支持 CSV / 粘贴文本 |
| 盈利模式 | MVP 不向用户收费 | v2.0 考虑高级功能订阅，非广告弹券 |

---

## 四、页面结构

### 4.1 MVP Tab 结构（3 Tab）

```
┌─────────────────────────────────────────┐
│              Tab 导航栏                  │
│       [记账]    [报表]    [我的]         │
├─────────────────────────────────────────┤
│                                         │
│  【记账 Tab】                            │
│  └── 首页 Dashboard                      │
│       本月结余 / 预算进度 / VS 上月       │
│       洞察卡片（1 条摘要）                │
│       快捷分类入口 × 6                    │
│       最近记账记录                        │
│       悬浮按钮「记一笔」（可收起）         │
│  └── 记账页 QuickRecord                  │
│       数字键盘 + 分类 + 默认性质           │
│       关键词分类预判（本地 Mock）           │
│       完成页内嵌预算反馈                  │
│  └── 账单导入 Import                     │
│       CSV 上传 / 文本粘贴解析             │
│  └── 分类管理 CategoryManage             │
│  └── 固定收支 Recurring                    │
│                                         │
│  【报表 Tab】                            │
│  └── 报表首页                            │
│       收支三栏 + 环形图 + 折线图          │
│  └── 支出明细（下钻）                    │
│  └── 数据导出                            │
│                                         │
│  【我的 Tab】                            │
│  └── 个人中心                            │
│       连续记账天数 + 存款目标             │
│       成就徽章（8 个）                    │
│  └── 预算设置（含 onboarding 入口）      │
│  └── 分类管理 / 固定收支 / 提醒设置      │
│  └── 隐私与数据 / 账号注销               │
│                                         │
└─────────────────────────────────────────┘
```

### 4.2 完整愿景 Tab 结构（v2.0，5 Tab）

在 MVP 3 Tab 基础上新增：

- **洞察 Tab**（替代原「攻略」）：消费习惯提醒、超支预警历史、外链优惠（无爬虫）
- **工具 Tab**：轻量房贷/车贷计算器

---

## 五、功能模块详解

### 5.1 日常记账模块

**核心目标：** 快（≤5 秒手动录入）、准（即时反馈）、少（≤10 主类）

#### 5.1.1 首页 Dashboard

```
┌─────────────────────────────────────────┐
│  ╭──────────────────────────────╮        │
│  │ 👋 吴银龙，今天是 6 月 6 日   │        │
│  │                              │        │
│  │  本月可支配       ¥3,450     │        │
│  │  ▓▓▓▓▓▓▓▓░░░░  已花¥2,550  │        │
│  │                              │        │
│  │  📉 比上月同期少花 ¥320 👍   │        │
│  │  🟡 可选预算：剩 ¥280 / 月均¥500│      │
│  ╰──────────────────────────────╯        │
│                                         │
│  ┌─ 💡 今日洞察 ─────────────────────┐  │
│  │ 餐饮本周已花 ¥420，比均值高 18%    │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────┐ ┌───────┐ ┌───────┐          │
│  │  🍜   │ │  🚕   │ │  🛒   │          │
│  │ 餐饮   │ │ 出行   │ │ 购物   │          │
│  │ ¥560  │ │ ¥120  │ │ ¥380  │          │
│  └───────┘ └───────┘ └───────┘          │
│  ...（共 6 个快捷入口）                  │
│                                         │
│  最近：购物¥128  餐饮¥38  出行¥22       │
│                                         │
│              ╭──────╮                    │
│              │＋记一笔│                   │
│              ╰──────╯                    │
└─────────────────────────────────────────┘
```

**洞察卡片规则（MVP）：** 每日最多展示 1 条，优先级：严重超支预警 > 分类异常增长 > 正向鼓励（比上月少花）。

#### 5.1.2 记账页 QuickRecord

**录入流程（默认路径 ≤3 步）：**

1. 输入金额
2. 点选分类（记忆上次分类，可一键沿用）
3. 点 ✓ 确认

**性质三档策略（v2.0 修正）：**

- 记账时 **默认「可选」**，界面不强制展示三档选择（减少干扰）
- 点击「更多」可展开：🔴 刚需 / 🟡 可选 / 🟢 奢侈
- 完成页提供一键修改性质

```
┌─────────────────────────────────────────┐
│  ← 返回        记一笔           [导入]   │
├─────────────────────────────────────────┤
│                                         │
│            ¥ 128.00                     │
│                                         │
│    购物 · 上次                            │
│                                         │
├─────────────────────────────────────────┤
│  🍜  │ 🚕  │ 🛒  │ 💡  │ 📱 │ ··· │    │
│  餐饮│ 出行│ 购物│ 账单│ 通讯│其他  │    │
├─────────────────────────────────────────┤
│  备注（可选）：永辉超市                   │
│  💡 预判：购物  [✓] [改]                 │
├─────────────────────────────────────────┤
│  [数字键盘]                              │
└─────────────────────────────────────────┘
```

**关键词分类预判（MVP）：** 本地规则表匹配商家名，不调大模型。例：麦当劳→餐饮，永辉→购物。

#### 5.1.3 记账完成反馈（内嵌，非全屏弹窗）

```
┌─────────────────────────────────────────┐
│  ✅ 已记录                               │
│                                         │
│  🛒 购物 · 可选  ¥128.00                │
│  ────────────────────                   │
│  本月购物预算剩余：¥ 372  │  已用 77%    │
│  ████████░░░░                           │
│  🟡 可选预算剩余：¥ 152  │  已用 78%    │
│                                         │
│  [改性质]  [完成]  [再记一笔]             │
└─────────────────────────────────────────┘
```

**超支处理：** 不禁止记账。超支时二次确认：「本月购物已超 ¥50，仍要记录吗？」并可选填超支原因。

#### 5.1.4 账单导入（MVP 新增）

**入口：** 记账页右上角「导入」/ 我的 → 数据管理 → 导入账单

| 方式 | 说明 |
|------|------|
| CSV 上传 | 支持微信、支付宝官方导出格式 |
| 文本粘贴 | 粘贴账单列表，正则 + 规则解析金额/商户/时间 |
| 去重策略 | 金额 + 时间（±5 分钟）+ 商户名模糊匹配 |

导入后进入 **待确认列表**，用户批量确认分类后入账（避免全自动错分）。

#### 5.1.5 分类体系（10 个主类）

| # | 分类 | Emoji | 说明 |
|---|------|-------|------|
| 1 | 餐饮 | 🍜 | 三餐、零食、饮料 |
| 2 | 出行 | 🚕 | 打车、公交、地铁、油费 |
| 3 | 购物 | 🛒 | 日用品、超市、网购 |
| 4 | 账单 | 💡 | 水电煤、房租、物业 |
| 5 | 通讯 | 📱 | 手机费、宽带 |
| 6 | 娱乐 | 🎮 | 游戏、电影、聚会 |
| 7 | 服饰 | 👕 | 服装、鞋帽、护肤品 |
| 8 | 医疗 | 🏥 | 买药、看病 |
| 9 | 人情 | 🎁 | 红包、送礼、请客 |
| 10 | 其他 | ··· | 未分类支出 |

**性质三档：**

| 档位 | 名称 | 预算控制 |
|------|------|----------|
| 🔴 | 刚需 | 不设上限，仅统计 |
| 🟡 | 可选 | 年度可选预算池 |
| 🟢 | 奢侈 | 月度奢侈阈值（可选配置） |

#### 5.1.6 固定收支 Recurring

- 工资自动入账：设置到账日/金额，到期自动生成收入记录
- 固定花销循环：房租、订阅等到期自动生成，到期前 1 天提醒
- 日历视图：展示当月所有固定收支节点

---

### 5.2 预算与 Onboarding

#### 5.2.1 新用户引导（MVP 新增，≤3 屏）

```
屏 1：欢迎
  「记完就知道还能花多少」
  [开始设置]  [先逛逛，稍后设置]

屏 2：收入与模板
  月收入  [¥8,000]
  选择模板：[工薪族] [租房党] [自定义]
  → 自动填充分类预算建议值

屏 3：可选预算
  年度可选预算  [¥6,000]（默认 = 月收入 × 15% × 12，可改）
  预警时机  [消耗 80% 时提醒 ●]
  [完成，记第一笔]
```

**跳过引导：** 允许「先逛逛」，但首页顶部展示轻量提示条引导完成预算设置。

#### 5.2.2 预算规则

| 类型 | 配置 | 预警 |
|------|------|------|
| 分类月预算 | 每分类独立设置 | 80% 橙色提示；100% 超支确认 |
| 可选年度预算 | 全年可选支出池 | 80% 首页洞察；100% 完成页强提示 |
| 奢侈月预算 | 可选配置 | 超支时完成页提示，不阻断 |

**取消 v1.0「100% 禁止记账」。**

---

### 5.3 图表分析模块（MVP 范围）

#### 5.3.1 报表首页

- 收支结余三栏卡片（含同比/环比百分比）
- 支出占比环形图（点击下钻明细，长按快捷设预算 — v1.1）
- 近 6 月支出折线图（整体 + 餐饮/购物双线）
- 月度预测简版：预计全月支出 + 是否可能超支（基于已发生 + 固定收支 + 近 3 月均值）

#### 5.3.2 支出明细下钻

- 按分类查看逐笔列表
- 筛选：性质（刚需/可选/奢侈）、日期、金额区间
- 排序：时间 / 金额
- 左滑删除、右滑编辑

#### 5.3.3 延后至 v1.1+

- 周报 / 月报 / 年报
- 日历热力图
- 分享报告图片

---

### 5.4 洞察中心（替代原「省钱快报弹窗」）

#### 5.4.1 设计原则

- **一个引擎、三种触达形态**，避免 18 套独立弹窗 UI
- 默认不打断用户心流；高优先级才主动触达

#### 5.4.2 触达形态

| 形态 | 场景 | 示例 |
|------|------|------|
| 内嵌反馈 | 记账完成后 | 预算剩余、超支确认 |
| 首页卡片 | 每日 1 条 | 分类支出异常、正向鼓励 |
| 洞察 Inbox（v1.1） | 历史洞察归档 | 未读/已读，可一键关闭同类 |

#### 5.4.3 规则配置表（18 条规则保留为配置，UI 统一）

规则字段：`id | 触发条件 | 内容模板 | 优先级 | 触达形态 | 静默策略`

| 优先级 | 触达方式 | 示例规则 |
|--------|----------|----------|
| 强制 | 完成页强提示条 | 可选预算耗尽、月度预测超支 30% |
| 高 | 完成页内嵌 + 首页卡片 | 分类超 80%、单笔大额 |
| 中 | 仅首页卡片 / Inbox | 消费趋势异常、未记账 3 天 |
| 低 | 仅 Inbox | 比上月同期多/少花 |

**静默策略：** 同类规则每日最多触达 1 次（强制级除外）；用户可「今日不再提示此类」。

#### 5.4.4 v2.0 省钱洞察（非导购）

不做爬虫比价，改为：

- 「你常去永辉，周三有会员日」— 基于消费频次 + 公开信息
- 「本月餐饮比 3 个月均值高 22%」— 基于账单
- 外链按钮跳转美团/京东（官方 Deep Link，无价格承诺）

---

### 5.5 AI 功能（分阶段）

#### MVP：不接入大模型

- 关键词规则表实现分类预判
- 模板化洞察文案（「本周餐饮 ¥{amount}，比均值高 {percent}%」）

#### v1.2：AI 财务教练

| 场景 | 频率 | 数据输入 |
|------|------|----------|
| 周报解读 | 每周 1 次 | 周聚合：分类金额、同比 |
| 月报解读 | 每月 1 次 | 月聚合 + 预算达成率 |
| 异常问答 | 用户主动，每周 ≤3 次 | 用户问题 + 近 3 月聚合 |

**隐私：** 不传商家明细、备注原文；接口层脱敏；隐私政策明示。

**不做（v2.0 前）：** 旅行规划、聚会方案、实时通用闲聊。

---

### 5.6 成就体系（MVP：8 个）

| 徽章 | 名称 | 解锁条件 | 周期 |
|------|------|----------|------|
| 🎊 | 记账新秀 | 完成新手引导 | 即时 |
| ✏️ | 第一笔 | 完成首笔记账 | 即时 |
| 🔥 | 初来乍到 | 连续记账 3 天 | 短 |
| 🔥 | 一周坚持 | 连续记账 7 天 | 短 |
| 🔥 | 月度满勤 | 连续记账 30 天 | 中 |
| 💎 | 精打细算 | 单月不超可选预算 | 中 |
| 📉 | 越来越省 | 连续 2 月支出环比下降 | 中 |
| 🎯 | 说到做到 | 达成月度存款目标 | 长 |

v1.2 扩展至 15 个；完整愿景最多 20 个（不超过 v1.0 的 23 个）。

---

### 5.7 财务工具（v2.0 延后）

MVP 不提供独立 Tab。v2.0 仅保留：

- 房贷/车贷月供计算器（等额本息/等额本金，利率手动输入）
- 计算结果可「同步为固定支出」

**不做：** 实时房价、车价、利率爬取（运维成本高，与定位弱相关）。

---

### 5.8 家庭共享（v1.2 / v2.0）

| 阶段 | 能力 |
|------|------|
| MVP | 数据模型预留 `memberId`，不实现 UI |
| v1.2 | 户主邀请成员 **只读** 查看家庭汇总 |
| v2.0 | 成员协作记账，各自记账、汇总查看 |

---

## 六、数据模型

### 6.1 records（记账记录）

```json
{
  "id": "string",
  "userId": "string",
  "amount": "number",
  "type": "expense | income",
  "category": "string",
  "subCategory": "string",
  "nature": "刚需 | 可选 | 奢侈",
  "remark": "string",
  "aiTag": "string",
  "source": "manual | import | recurring",
  "importBatchId": "string?",
  "timestamp": "number",
  "isRecurring": "boolean",
  "recurringId": "string?",
  "memberId": "string?",
  "createdAt": "number",
  "updatedAt": "number"
}
```

### 6.2 categories（分类配置）

```json
{
  "id": "string",
  "userId": "string",
  "name": "string",
  "emoji": "string",
  "iconColor": "string",
  "budget": "number",
  "budgetWarnPercent": "number",
  "isVisible": "boolean",
  "order": "number",
  "subCategories": ["string"]
}
```

### 6.3 optionalBudget（可选预算）

```json
{
  "userId": "string",
  "year": "number",
  "annualLimit": "number",
  "warnPercent": "number",
  "currentUsage": "number"
}
```

### 6.4 recurringItems（固定收支）

```json
{
  "id": "string",
  "userId": "string",
  "name": "string",
  "amount": "number",
  "type": "income | expense",
  "category": "string",
  "repeatRule": "monthly | weekly | yearly",
  "dayOfMonth": "number",
  "isActive": "boolean",
  "lastTriggered": "number",
  "nextTrigger": "number"
}
```

### 6.5 achievements（成就记录）

```json
{
  "id": "string",
  "userId": "string",
  "code": "string",
  "unlockedAt": "number",
  "notified": "boolean"
}
```

### 6.6 importBatches（账单导入批次，MVP 新增）

```json
{
  "id": "string",
  "userId": "string",
  "source": "wechat | alipay | paste",
  "fileName": "string",
  "totalCount": "number",
  "importedCount": "number",
  "status": "pending | confirmed | failed",
  "createdAt": "number"
}
```

### 6.7 insights（洞察记录，v1.1 新增）

```json
{
  "id": "string",
  "userId": "string",
  "ruleId": "string",
  "title": "string",
  "content": "string",
  "priority": "low | medium | high | critical",
  "channel": "inline | card | inbox | push",
  "readAt": "number?",
  "dismissedAt": "number?",
  "createdAt": "number"
}
```

### 6.8 users（用户）

```json
{
  "id": "string",
  "wxOpenId": "string",
  "phone": "string?",
  "nickname": "string",
  "avatar": "string",
  "monthlyIncome": "number?",
  "savingsGoal": "number?",
  "onboardingCompleted": "boolean",
  "createdAt": "number"
}
```

---

## 七、隐私与合规（MVP 必备）

| 事项 | 要求 |
|------|------|
| 隐私政策 | 首次启动展示，明示收集字段与用途 |
| 数据存储 | 国内云服务，传输 HTTPS |
| 账号注销 | 我的 → 隐私与数据 → 注销账号，7 日内清除云端数据 |
| 数据导出 | 支持导出 CSV（记账记录） |
| 离线冲突 | 以 `updatedAt` 较新者为准；冲突时保留两条并标记待用户合并 |
| AI（v1.2） | 仅传聚合统计，用户可关闭 AI 功能 |
| 权限 | 定位（v2.0 前不申请）、相册（导入 CSV）、通知（可选） |

---

## 八、交互规范

### 8.1 手势

| 页面 | 手势 | 动作 |
|------|------|------|
| 首页 | 下拉 | 刷新数据 |
| 环形图 | 点击 | 下钻明细 |
| 明细列表 | 左滑 | 删除 |
| 明细列表 | 右滑 | 编辑 |
| 洞察卡片 | 右滑 | 关闭今日此类 |

### 8.2 动画规范

| 场景 | 动画 | 时长 |
|------|------|------|
| 页面切换 | 滑动 | 300ms |
| 完成页出现 | 底部滑入 | 250ms |
| 按钮点击 | 缩放 0.95→1 | 150ms |
| 数字变化 | 翻转 | 400ms |
| 记账成功 | 打勾 | 300ms |

### 8.3 速度指标

| 操作 | 目标 |
|------|------|
| 点击记一笔 → 键盘可用 | ≤ 0.5s |
| 手动记账（金额+分类+确认） | ≤ 5s |
| 记账完成 → 反馈展示 | ≤ 0.3s |
| 首屏加载 | ≤ 1.5s（骨架屏） |

---

## 九、性能要求

| 指标 | 要求 |
|------|------|
| 页面首屏 | ≤ 1.5s |
| 图表渲染 | ≤ 500ms（< 500 条） |
| 切换月份 | ≤ 300ms |
| 离线模式 | 缓存最近 200 条，离线可记账 |
| 导入解析 | 1000 条 < 3s（客户端） |

---

## 十、核心指标（北极星）

**北极星指标：** 周活跃记账天数（每周至少记账 1 天的用户占比）

| 指标 | MVP 目标（上线 30 天） |
|------|------------------------|
| 次日留存 | > 40% |
| 7 日留存 | > 20% |
| 人均周记账笔数 | ≥ 5 笔 |
| 记账后反馈查看率 | > 60% |
| 预算设置完成率（7 日内） | > 50% |
| 账单导入使用率 | > 15%（导入过至少 1 次的用户占比） |

### 埋点事件（MVP）

| 事件 | 参数 |
|------|------|
| record_create | source, category, nature, duration_ms |
| record_feedback_view | category, over_budget |
| budget_onboarding_complete | template_type |
| import_start / import_confirm | source, count |
| insight_show / insight_dismiss | rule_id, channel |
| achievement_unlock | code |

---

## 十一、开发排期（修正版）

### MVP v1.0 — 6–8 周（单人开发参考）

| 周次 | 内容 | 交付物 |
|------|------|--------|
| W1–2 | P0 基础搭建 + 登录 + 数据模型 + 3 Tab 骨架 | 可运行空壳 |
| W3–4 | 记账页 + 即时反馈 + 分类 + 预算 onboarding | 核心记账闭环 |
| W5 | 固定收支 + 账单导入 | 低摩擦录入 |
| W6 | 报表首页 + 环形图 + 折线图 + 下钻 | 数据可视化 |
| W7 | 成就 8 个 + 洞察卡片 + 设置 + 合规 | 功能完整 |
| W8 | 联调、性能、小程序提审 buffer | MVP 上线 |

**MVP 工时估算：** 约 35–40 人天（含 1 周 buffer）

### 后续版本

| 版本 | 周期 | 核心增量 |
|------|------|----------|
| v1.1 | +4 周 | 语音记账、周报月报、洞察 Inbox |
| v1.2 | +4 周 | AI 教练、家庭只读、成就 15 个 |
| v2.0 | +6 周 | 洞察 Tab、轻量计算器、联盟优惠 |

---

## 十二、风险与应对（更新）

| 风险 | 应对 |
|------|------|
| 手动记账疲劳 | MVP 上线账单导入；v1.1 语音 + 支付提醒 |
| 微信小程序企业主体 | 叶总配合资质；过渡期可用个人测试号局限功能 |
| 预算设置门槛高 | 模板引导 + 允许跳过 + 收入比例默认值 |
| AI 成本 | MVP 不用大模型；v1.2 仅周期性聚合调用 |
| 功能膨胀 | 严格按版本规划评审，新需求先入 Backlog |
| 文档分叉 | 以本文档 v2.0 为唯一 Source of Truth |

---

## 十三、设计文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `家计通-完整产品设计文档-v2.md` | **当前有效** | 本文档（修正版母文档） |
| `家计通-产品设计评估报告.md` | 当前有效 | 评估依据 |
| `家计通-开发任务分解表-v2.md` | 当前有效 | MVP 排期（约 37 人天） |
| `家计通-完整产品设计文档.md` | 已归档 | v1.0，仅供参考 |
| `01~05 模块设计稿` | 已归档 | 内容已合并入 v2.0 |

---

> 设计：夏雨 | 评估修订：2026-06-06 | 产品审核：叶纬 | 开发确认：吴银龙
